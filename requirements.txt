# ===== Smart Document Organizer - 后端依赖 =====
# 核心框架
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-multipart>=0.0.9
aiofiles>=23.2

# 文件解析
PyMuPDF>=1.24        # PDF 文本/图像提取
python-docx>=1.1     # DOCX 解析
openpyxl>=3.1        # XLSX 解析
Pillow>=10.0         # 图像处理

# OCR（README/规格书声明的默认引擎，代码在 services/ocr_service.py 中按需导入）
rapidocr-onnxruntime>=1.3     # PP-OCR 系模型，ONNX 本地运行

# HTTP 客户端（AI Provider / 测试）
httpx>=0.27

# 测试
pytest>=8.0
