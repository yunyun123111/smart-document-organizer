# 智能文档自动整理系统 · 产品说明

## 1. 产品简介

智能文档自动整理系统是一款**本地运行**的 AI 文档管理工具：把杂乱文件放入指定位置，系统自动识别文件类型、提取关键信息、按分类重命名并归档；拿不准的文件交给人工确认，全程留痕、可撤销、绝不误删。

> 💡 **核心理念：** 让机器处理确定的事情，让人处理机器不确定的事情——规则引擎优先、AI 智能兜底、置信度分档、人工审核把关。

## 2. 功能模块

| 模块 | 功能说明 |
| --- | --- |
| 数据看板 | 文档总数、已归档、待人工确认、今日新增等统计；文件类型分布；系统状态（OCR / AI 是否启用）。点击类型可跳转查看明细。 |
| 文件整理 | 上传文件或扫描待整理目录，批量执行自动识别、重命名、分类、归档。 |
| 待人工确认 | 置信度不足的文件逐条人工审核，可修改类型、分类、识别字段和文件名；支持批量确认归档、批量移除误传文件、跳过。 |
| 文档库 | 已归档文件检索与管理：按类型 / 分类 / 状态筛选、关键词搜索、手动重命名、查看详情、打开原件、删除；支持批量导出压缩包、批量删除。 |
| 分类管理 | 维护分类树、关键词规则和命名模板，控制自动归档的规则。 |
| 操作日志 | 记录每一次识别、归档、重命名、删除等操作，支持对归档操作撤销恢复。 |
| 系统设置 | 访问密码、AI 接口配置、本地 OCR 开关、手机邮箱接收、文件名规则、置信度阈值等。 |

## 3. 页面布局

系统采用**左侧导航 + 顶部标题 + 主内容区**的后台式布局：左侧为全部功能模块菜单，点击即切换页面；顶部显示当前页面名称。

- **电脑端：** 左侧固定导航栏，主内容区展示数据看板、表格、审核列表等。
- **手机端：** 左侧导航自动折叠为「☰」菜单按钮，点开抽屉选择页面；列表自动适配为卡片式，方便触屏操作。

## 4. 快速使用

### 4.1 启动系统

1. 在软件目录双击**「启动.bat」**，等待窗口提示服务已启动。
2. 浏览器会自动打开系统首页；若未打开，手动访问 **http://127.0.0.1:8000**。

> ⚠️ 关闭启动窗口即停止系统。请保持窗口常开。

### 4.2 登录系统

如果管理员设置了访问密码，首次进入需输入密码；输入正确后即可使用。未设置密码则直接进入。

### 4.3 放入文件（三种方式）

1. **网页上传：** 进入「文件整理」页，选择文件上传。
2. **目录投放：** 把文件复制进系统的「待整理目录」（收件箱），点「开始整理」。
3. **手机发送：** 手机浏览器访问局域网地址直接上传，或把文件作为附件发到指定邮箱，系统自动收取并识别。

### 4.4 自动识别与归档

系统会判断文件类型（合同、发票、结算单等）、提取合同号、船名、金额、日期等关键字段，按「日期_船名_合同号_物料_结算单」等规则重命名，并归入对应分类目录（如 财务/发票/2026/02）。识别结果清晰、可追踪。

### 4.5 人工确认

识别置信度不足的文件会进入「待人工确认」。逐条查看建议分类与识别字段，修改后「确认归档」；误传的文件可「批量移除」删除。

### 4.6 文档库管理

归档后的文件统一在「文档库」查看。可按类型筛选、搜索关键词（如公司名、合同号），需要时可批量导出为压缩包，或手动修正文件名。

## 5. 手机使用

手机与电脑连接**同一 WiFi** 时，用手机浏览器访问电脑的局域网地址（形如 http://192.168.x.x:8000，启动窗口会提示），即可上传文件、查看文档库、处理待确认项。

不在同一网络时，可使用**邮箱接收**：在「系统设置」中配置邮箱（IMAP 地址、账号、授权码），之后把文件作为附件发给该邮箱，系统会自动收取、识别并归档。

## 6. 数据与安全

- **数据本地保存：** 所有文档存于软件目录下，文档不经过第三方服务器。
- **访问密码：** 开放局域网访问时建议在「系统设置」中设置访问密码，防止他人访问。
- **AI 配置：** 如需 AI 辅助识别，在「系统设置」填入 AI 接口地址与 Key；不配置时自动降级为规则识别，不影响使用。
- **OCR：** 本地 OCR 默认启用，扫描件、图片发票也能识别内容，免费、不联网。
